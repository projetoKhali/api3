export default interface User {
  key: string;
  name: string;
  registration: string;
  userType: string;
  email: string;
  password: string;
  active: string;
  insertDate: string;
  expireDate: string;
}
